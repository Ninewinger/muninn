fastapi>=0.104.0
uvicorn[standard]>=0.24.0
pydantic>=2.0.0
requests>=2.31.0
numpy>=1.24.0

# Embedding backends (pick one):
# sentence-transformers>=2.2.0   # local embeddings (MiniLM, etc.)
# torch                          # required for sentence-transformers
# transformers>=4.36.0           # for Qwen3-Embedding

# Vector search extension
sqlite-vec>=0.1.0

# Optional: cross-encoder reranker
# pip install sentence-transformers # includes CrossEncoder